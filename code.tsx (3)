'use client'
import { useState, useEffect } from 'react'
import { motion } from 'framer-motion'
import { DollarSign, CheckCircle, Clock, Users, Star, Download } from 'lucide-react'
import Link from 'next/link'
import DepositModal from '@/components/DepositModal'

export default function Dashboard() {
  const [balance, setBalance] = useState(125.50)
  const [showDeposit, setShowDeposit] = useState(false)
  const [vipLevel, setVipLevel] = useState(2)

  const tasks = [
    { id: 1, title: 'متابعة حساب إنستغرام', reward: 2.5, completed: false },
    { id: 2, title: 'إعجاب 10 منشورات', reward: 1.8, completed: true },
    { id: 3, title: 'مشاركة منشور', reward: 3.2, completed: false },
    { id: 4, title: 'تسجيل في موقع', reward: 4.5, completed: false },
    { id: 5, title: 'كتابة تعليق', reward: 2.0, completed: true }
  ]

  return (
    <div className="min-h-screen pb-12">
      <div className="container mx-auto px-4 py-12">
        {/* Balance Card */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          className="glass p-8 rounded-3xl mb-12"
        >
          <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-6">
            <div>
              <p className="text-gray-400 mb-2">الرصيد المتاح</p>
              <div className="text-4xl md:text-5xl font-bold gradient-text">
                ${balance.toLocaleString()}
              </div>
            </div>
            <div className="flex gap-4">
              <button
                onClick={() => setShowDeposit(true)}
                className="btn-primary flex items-center gap-2 px-6 py-3"
              >
                <DollarSign size={20} />
                إيداع
              </button>
              <button className="btn-secondary flex items-center gap-2 px-6 py-3">
                <Download size={20} />
                سحب
              </button>
            </div>
          </div>
          
          {/* VIP Badge */}
          <div className="flex items-center gap-3 mt-8 p-4 glass rounded-2xl">
            <div className="flex gap-1">
              {[...Array(vipLevel)].map((_, i) => (
                <Star key={i} className="w-5 h-5 fill-yellow-400 text-yellow-400" />
              ))}
            </div>
            <span className="font-semibold text-purple-400">VIP {vipLevel} - زيادة الأرباح 10%</span>
          </div>
        </motion.div>

        <div className="grid lg:grid-cols-3 gap-8">
          {/* Daily Tasks */}
          <motion.div
            initial={{ opacity: 0, x: -20 }}
            animate={{ opacity: 1, x: 0 }}
            className="lg:col-span-2"
          >
            <div className="glass p-8 rounded-3xl">
              <h2 className="text-3xl font-bold mb-8 flex items-center gap-3">
                <CheckCircle className="text-green-400" />
                المهام اليومية
              </h2>
              <div className="space-y-4">
                {tasks.map((task) => (
                  <div key={task.id} className="glass p-6 rounded-2xl flex justify-between items-center group hover:bg-white/10 transition-all">
                    <div className="flex items-center gap-4">
                      <div className={`w-12 h-12 rounded-2xl flex items-center justify-center ${task.completed ? 'bg-green-500/20 text-green-400' : 'bg-purple-500/20 text-purple-400'}`}>
                        {task.completed ? <CheckCircle size={24} /> : <Clock size={24} />}
                      </div>
                      <div>
                        <h3 className="font-semibold">{task.title}</h3>
                        <p className="text-green-400 font-bold">${task.reward}</p>
                      </div>
                    </div>
                    <button
                      disabled={task.completed}
                      className={`px-6 py-2 rounded-xl font-semibold transition-all ${
                        task.completed
                          ? 'bg-green-500/20 text-green-400 cursor-not-allowed'
                          : 'btn-primary hover:scale-105'
                      }`}
                    >
                      {task.completed ? 'مكتملة' : 'ابدأ المهمة'}
                    </button>
                  </div>
                ))}
              </div>
            </div>
          </motion.div>

          {/* Quick Stats */}
          <motion.div
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            className="space-y-6"
          >
            <div className="glass p-6 rounded-2xl">
              <h3 className="font-semibold mb-4">الإحصائيات</h3>
              <div className="space-y-4 text-sm">
                <div className="flex justify-between">
                  <span>المهام المنجزة</span>
                  <span className="font-bold">127</span>
                </div>
                <div className="flex justify-between">
                  <span>الإجمالي المكتسب</span>
                  <span className="font-bold text-green-400">$1,247</span>
                </div>
                <div className="flex justify-between">
                  <span>المدعوين</span>
                  <span className="font-bold">23</span>
                </div>
              </div>
            </div>

            <div className="glass p-6 rounded-2xl">
              <h3 className="font-semibold mb-4">الإحالات</h3>
              <div className="text-center">
                <div className="text-2xl font-bold gradient-text mb-2">$23</div>
                <p className="text-gray-400 text-sm">أرباح الإحالة</p>
                <button className="mt-4 w-full btn-secondary text-sm py-2">
                  مشاركة الرابط
                </button>
              </div>
            </div>
          </motion.div>
        </div>
      </div>

      <DepositModal
        isOpen={showDeposit}
        onClose={() => setShowDeposit(false)}
        amount={50}
      />
    </div>
  )
}
